import AboutClient from './AboutClient';

export const metadata = {
  title: "About FlashTTS — Built for Creators Who Never Stop Publishing",
  description: "FlashTTS was built because ElevenLabs kept running out of credits. We're on a mission to give every creator unlimited studio-quality AI voices at a price that makes sense.",
};

export default function AboutPage() {
  return <AboutClient />;
}
